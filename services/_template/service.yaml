id: example
name: Example Service
status: template
implementation:
  type: external
  repository: https://github.com/example/example-cli
  package: example-cli
  command: example
contract:
  stdout: single-json-document
  stderr: diagnostics-only
  writes_default_to_dry_run: true
  apply_flag: --apply
  secrets_in_output: forbidden
openwiki:
  notes: openwiki/decisions/service-cli-contract.md
